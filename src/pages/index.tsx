import { Flex } from "@chakra-ui/react";
import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <Flex>
      <Head>
        <title>Home</title>
      </Head>
    </Flex>
  );
};

export default Home;
